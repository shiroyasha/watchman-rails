require "spec_helper"

RSpec.describe Watchman::Rails do
  it "has a version number" do
    expect(Watchman::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
